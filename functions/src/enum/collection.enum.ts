enum Collections {
    TASKS = "tasks",
}

enum Conditional {
    STATUS = "status",
}

enum StatusTask {
    ALL = "ALL",
    OPEN = "OPEN",
    PROGRESS = "PROGRESS",
    TESTING = "TESTING",
    DONE = "DONE",
}

export {Collections, StatusTask, Conditional}